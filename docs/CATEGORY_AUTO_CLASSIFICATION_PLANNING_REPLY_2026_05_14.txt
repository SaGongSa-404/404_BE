카테고리 자동분류는 유저가 직접 선택만 하게 하기보다는, 자동으로 먼저 선택해두고 유저가 필요하면 수정할 수 있게 가도 될 것 같습니다.

백엔드에서 상품 링크로 상품명, 설명, 본문 키워드를 가져와서 카테고리를 분류하는 방식으로 보면, 크롤링 자체는 크게 문제 없었습니다. 브라우저 기반으로 수집했을 때 검색/카테고리 URL까지 포함해서 70개 중 70개가 정상 수집됐고, 분류 정확도는 92.9%였습니다.

다만 검색 URL은 검색어 자체가 카테고리 힌트로 들어갈 수 있어서 실제 상품 저장 상황보다 정확도가 높게 나올 수 있습니다. 실제 유저가 상품 상세 링크를 저장하는 상황에 더 가까운 기준으로 보면, 상품 상세 URL 58개 중 58개가 정상 수집됐고 분류 정확도는 81.0%였습니다.

그래서 실제 상품 저장 기준으로는 자동분류 정확도를 보수적으로 80% 초반 정도로 보면 될 것 같습니다.

현재 기획 카테고리가 패션, 뷰티, 라이프, 디지털, 기타 5개 대분류라서, 테스트에 쓴 세부 분류보다 난이도는 낮습니다. 식품, 취미, 생활용품 쪽이 라이프나 기타로 묶이면 실제 운영 기준 정확도는 81.0%보다 조금 더 올라갈 수 있습니다.

구현은 상품 링크 수집 시 상품명, 설명, 본문 키워드를 추출하고, 이걸 기반으로 카테고리를 1차 추천하는 방식으로 가면 될 것 같습니다. 분류 근거가 약하거나 여러 카테고리가 섞이는 경우에는 기타로 처리하거나 낮은 신뢰도 상태로 넘기고, 프론트에서는 추천된 카테고리를 기본 선택값으로 보여준 뒤 유저가 수정할 수 있게 하면 됩니다.

정리하면, 카테고리 자동분류를 확정값으로 쓰기보다는 추천값으로 제공하는 방향이면 충분히 가능해 보입니다. 직접 선택만 받는 방식보다는 자동선택 후 수정 가능 구조로 가는 쪽을 추천합니다.
