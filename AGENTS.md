# AGENTS.md

## 목적

- 이 저장소에서는 사용자가 요청한 범위까지만 수행한다.
- 테스트, 영구 코드 변경, PR, 머지, 배포, 서버 조작은 서로 다른 권한으로 취급한다.
- 모호한 요청을 더 큰 작업 권한으로 확대 해석하지 않는다.

## 요청 해석과 허용 범위

- `확인`, `설명`, `분석`, `알려줘`, `답변만`은 읽기 전용 요청이다. 파일, GitHub, 서버 상태를 변경하지 않는다.
- `테스트`, `한 번 해봐`, `동일하게 날려봐`는 테스트 실행만 허용한다. 테스트에 꼭 필요한 일시적 설정은 실행 종료 후 즉시 원복한다.
- 테스트 요청만으로 제품 코드 수정, 이슈 생성, 브랜치 생성, 커밋, 푸시, PR, 머지, 배포를 수행하지 않는다.
- `수정`, `구현`, `반영`, `작업 진행`은 명시된 코드 변경과 검증을 허용한다. 저장소 컨벤션상 이슈·브랜치·커밋·PR이 필요하면 그 범위까지만 진행한다.
- `PR 올려` 또는 `PR 만들어`는 PR 생성까지다. 머지와 배포 권한을 포함하지 않는다.
- `머지`는 지정 PR의 머지만 허용한다. 배포 또는 서버 변경 권한을 포함하지 않는다.
- `배포`는 지정 환경 배포와 상태 확인까지만 허용한다. 부하 테스트, 데이터 생성, 운영 설정 변경을 포함하지 않는다.
- 서버 환경변수 변경, 프로세스 재시작, DB 변경, 대량 요청, 부하 테스트는 사용자가 해당 동작을 명시한 경우에만 수행한다.

## 모호하거나 공유 상태가 필요한 경우

- 로컬 또는 격리 환경에서 끝낼 수 없는 테스트라면 공유 QA나 서버를 바꾸기 전에 변경 대상, 지속 시간, 원복 방법을 짧게 알리고 승인을 받는다.
- `적용하고 테스트`처럼 영구 반영인지 임시 실험인지 구분되지 않으면 구현·머지하지 말고 먼저 범위를 확인한다.
- 되돌리기, 삭제, 강제 푸시처럼 파괴적인 작업은 대상이 명확히 지정된 경우에만 수행한다.
- 사용자가 중단하거나 범위를 축소하면 진행 중인 후속 작업을 즉시 멈춘다.

## GitHub 작업 규칙

- 사용자가 코드 작업과 PR 진행을 요청한 경우 기존 프로젝트 컨벤션에 따라 이슈, 브랜치, 커밋, PR 순서로 진행한다.
- 이슈와 PR을 만들거나 수정할 때 먼저 템플릿과 최근 작성 방식을 확인하고 제목, 본문, 체크리스트, 링크 형식을 맞춘다.
- 커밋 메시지는 한글로 작성한다. 기존 타입 접두어가 확인되면 접두어는 유지한다.
- 사용자가 지라키를 지정하지 않았다면 최근 원격 브랜치, 이슈, PR, 커밋에서 같은 프로젝트 키의 가장 큰 번호를 확인하고 1 증가시킨다.
- 지라키 접두어가 여러 개면 현재 작업 흐름에서 가장 최근 접두어를 우선하며, 별도 지시가 없으면 `NF`를 사용한다.
- 사용자가 지정한 지라키, 브랜치명, 이슈 번호, PR 대상은 자동 계산보다 우선한다.
- 최신 지라키를 확인할 수 없을 때만 사용자에게 묻는다.

## 완료 및 보고

- 완료 보고에서 `코드 변경`, `PR 생성`, `머지`, `배포`, `서버 검증` 상태를 분리해 사실대로 적는다.
- 실행하지 않은 테스트나 배포를 완료했다고 표현하지 않는다.
- 요청 범위 밖에서 유용해 보이는 후속 작업은 실행하지 않고 필요할 때 제안만 한다.
